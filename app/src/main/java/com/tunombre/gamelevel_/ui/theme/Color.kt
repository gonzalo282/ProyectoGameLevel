package com.tunombre.gamelevel_.ui.theme

import androidx.compose.ui.graphics.Color

// ===== Paleta base del mock =====
val PurpleBg     = Color(0xFF2C0F88) // Fondo principal de la pantalla
val PurpleAppBar = Color(0xFF5A2BDA) // AppBar / barra superior
val PurpleCard   = Color(0xFF3C17B3) // Contenedores/inputs tipo tarjeta

// Acentos
val ElectricBlue = Color(0xFF1E90FF) // Azul de énfasis (si lo necesitas)
val YellowAccent = Color(0xFFFFD000) // Botón amarillo principal
val FacebookBlue = Color(0xFF1877F2) // Botón Facebook

// Texto y ayudas
val OnDark  = Color(0xFFFFFFFF) // Texto principal (blanco)
val OnMuted = Color(0xB3FFFFFF) // Blanco 70% para labels/hints
val BgBlack = Color(0xFF101010) // Texto oscuro sobre el botón amarillo

// Bordes/separadores sutiles
val BorderSoft = Color(0x33FFFFFF) // Blanco 20% para bordes y dividers

